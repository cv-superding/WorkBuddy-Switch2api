[workspace]
members = ["crates/wb-switch-core", "crates/wb-switch-server", "src-tauri"]
resolver = "2"

# 发版只改这一处版本号；三个成员的 version 都继承它。
# 其余字段（authors/repository/license）刻意不继承 —— 各 crate 的 homepage
# 并不完全一致，抽出去反而容易改错。
[workspace.package]
version = "0.6.1"
